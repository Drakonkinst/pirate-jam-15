extends Resource

class_name Speaker

# We would normally handle speaker textures here, but we're hardcoding it
@export var id: String
@export var name: String
@export var text_color: Color = Color.WHITE